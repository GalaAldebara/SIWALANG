<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\Support\Facades\DB;

class LevelController extends Controller
{
    // public function index()
    // {
    //     DB::insert('insert into m_level(level_kode, level_nama, created_at) values(?,?,?)', ['ADM', 'Administrator', now()]);
    //     DB::insert('insert into m_level(level_kode, level_nama, created_at) values(?,?,?)', ['RW', 'Rukun Warga', now()]);
    //     DB::insert('insert into m_level(level_kode, level_nama, created_at) values(?,?,?)', ['RT', 'Rukun Tangga', now()]);
    //     DB::insert('insert into m_level(level_kode, level_nama, created_at) values(?,?,?)', ['WRG', 'warga', now()]);

    //     return 'insert data baru berhasil';
    // }
}
